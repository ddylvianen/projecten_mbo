{
  "name": "projecten",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch scss:css",
    "minify": "sass --style=compressed scss:css",
    "dev": "concurrently \"npm run sass\" \"live-server --open=index.html\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.1.0",
    "live-server": "^1.2.2",
    "sass": "^1.81.0"
  }
}
